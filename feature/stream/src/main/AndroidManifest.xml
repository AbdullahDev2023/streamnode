<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Audio streaming (existing) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Screen capture (Phase 1) — required for MediaProjection on API 29+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Camera streaming — required for foreground camera access on API 29+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />

    <!-- Feature 4: Telemetry — phone state for signal strength, location for Wi-Fi SSID on API 29+ -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application>
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <!--
          StreamingService — foregroundServiceType now includes mediaProjection
          so it can hold a MediaProjection token while screen sharing.
        -->
        <service
            android:name=".service.StreamingService"
            android:exported="false"
            android:foregroundServiceType="microphone|mediaProjection|camera"
            android:stopWithTask="false" />

        <!--
          StreamNodeAccessibilityService — Phase 2 input injection.
          User must enable this manually in Settings → Accessibility → StreamNode.
          BIND_ACCESSIBILITY_SERVICE permission is granted by the system only.
        -->
        <service
            android:name=".service.StreamNodeAccessibilityService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
